import { useState } from "react";import { useNavigate } from "react-router-dom";import API from "../api/api";
const getUser=()=>{try{return JSON.parse(localStorage.getItem("user"))}catch{return null}};
function AddCourse(){ const user=getUser(); const [title,setTitle]=useState(""); const [price,setPrice]=useState(""); const [description,setDescription]=useState(""); const [image,setImage]=useState(null); const navigate=useNavigate(); if(user?.role!=="admin") return <h2 className="text-center mt-5">Only Admin Can Add Course</h2>; const submit=async(e)=>{e.preventDefault(); const fd=new FormData(); fd.append("title",title); fd.append("price",price); fd.append("description",description); if(image) fd.append("image",image); try{await API.post("/courses",fd); alert("Course added"); navigate("/");}catch(err){alert(err.response?.data?.message||"Add failed")}}; return <div className="container py-5"><div className="card form-card shadow-lg mx-auto"><h2 className="mb-4 text-center">Add New Course</h2><form onSubmit={submit}><input className="form-control mb-3" placeholder="Course title" value={title} onChange={e=>setTitle(e.target.value)} required/><input type="number" className="form-control mb-3" placeholder="Price" value={price} onChange={e=>setPrice(e.target.value)} required/><textarea className="form-control mb-3" placeholder="Description" value={description} onChange={e=>setDescription(e.target.value)}></textarea><input type="file" className="form-control mb-3" onChange={e=>setImage(e.target.files[0])}/><button className="btn btn-primary btn-block">Add Course</button></form></div></div>}
export default AddCourse;
