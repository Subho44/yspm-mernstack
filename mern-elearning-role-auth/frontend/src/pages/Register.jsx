import { useState } from "react";import { useNavigate, Link } from "react-router-dom";import API from "../api/api";
function Register(){ const [form,setForm]=useState({name:"",email:"",password:"",role:"student"}); const navigate=useNavigate(); const change=e=>setForm({...form,[e.target.name]:e.target.value}); const submit=async(e)=>{e.preventDefault(); try{await API.post("/auth/register",form); alert("Register successful"); navigate("/login");}catch(err){alert(err.response?.data?.message||"Register failed")}}; return <div className="auth-page"><div className="card auth-card shadow-lg"><h2 className="text-center mb-4">Create Account</h2><form onSubmit={submit}><input name="name" onChange={change} className="form-control mb-3" placeholder="Name" required/><input name="email" type="email" onChange={change} className="form-control mb-3" placeholder="Email" required/><input name="password" type="password" onChange={change} className="form-control mb-3" placeholder="Password" required/><select name="role" onChange={change} className="form-control mb-3"><option value="student">Student</option><option value="admin">Admin</option></select><button className="btn btn-primary btn-block">Register</button></form><p className="text-center mt-3">Already have account? <Link to="/login">Login</Link></p></div></div> }
export default Register;
